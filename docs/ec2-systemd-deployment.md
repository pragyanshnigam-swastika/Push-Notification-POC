# EC2 + systemd deployment guide

This content has been folded into a single, more complete production
document: **[production-deployment-explained.md](./production-deployment-explained.md)**.

It contains everything that used to live in this file — the VPC
prerequisites, building the Linux binary, the runtime configuration file,
the `systemd` unit, starting/inspecting the service, and the producer
contract — plus the architecture rationale, explicit purpose of every AWS
component, cost breakdown, security hardening checklist, observability
guidance, deployment pipeline, scaling strategy, a testing tutorial, and a
POC-to-production migration checklist.

This file is kept in place only so existing links to it keep working.
